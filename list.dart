void main() {
  List<int> numers = [
    11,
  ];
  numers.add(223);
  // numers.remove(3);
  //numers.removeAt(3);
  numers.contains(66);
  numers.length;
  print(numers.isEmpty);
  numers.insertAll(0, [1, 2, 3, 4, 4, 5, 5, 56]);
  print(numers);
  print(numers.length);
}
